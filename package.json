{
  "name": "gaydients",
  "main": "index.js",
  "dependencies": {
    "chalk-rainbow": "^1.0.0",
    "concurrently": "^5.1.0",
    "express": "^4.17.1",
    "express-favicon": "^2.0.1",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "node index.js",
    "installFiles": "concurrently \"yarn install\" \"cd client && yarn install\"",
    "dev": "concurrently --kill-others \"node index.js\" \"cd client && npm start\"",
    "heroku-postbuild": "cd client && yarn install && yarn build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/novacourtois/gaydients"
  }
}
